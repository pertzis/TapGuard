export interface LogEntry {
    id: number,
    uid: string,
    name: string,
    tin: Date
    tout: Date
}
export interface LogEntryDto {
    id: number,
    uid: string,
    name: string,
    tin: Date
    tout: Date
}